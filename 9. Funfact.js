//funfact, if u check the type of for null datatype, it shows object, even though null is considered a primitive datatype, it is a bug left uncorrected due to logistics in the official javascript documentation
let x=null;
console.log(typeof(x));
// O/P
// object
